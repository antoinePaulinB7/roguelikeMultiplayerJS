{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "watch": "webpack --watch",
    "dev": "concurrently --kill-others \"webpack --watch\" \"npx live-server dist/\""
  },
  "devDependencies": {
    "concurrently": "^6.0.0",
    "css-loader": "^5.1.3",
    "style-loader": "^2.0.0",
    "webpack": "^5.26.2",
    "webpack-cli": "^4.5.0"
  },
  "dependencies": {
    "rot-js": "^2.1.5",
    "socket.io-client": "^4.0.0"
  }
}
